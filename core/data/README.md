# Data

This folder is local and almost entirely ignored by Git.

- `raw/` contains supplied files. Never modify them.
- `processed/` contains rebuildable audits, manifests, statistics, and the single
  shared split at `processed/splits.csv`.

Only these guide files are tracked.
