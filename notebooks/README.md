# Notebooks

Use notebooks to tell the project story and show results.

Keep reusable loading, training, evaluation, and plotting code in `src/fashion/`.
Number notebooks in reading order.

`00_eda.ipynb` reads generated evidence. Rebuild that evidence with
`./.venv/bin/python scripts/generate_eda.py` before reviewing the story.
